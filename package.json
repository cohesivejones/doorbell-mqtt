{
  "name": "mqtt",
  "version": "1.0.0",
  "private": true,
  "main": "server/index.js",
  "author": "Nathan Jones",
  "license": "ISC",
  "engines": {
    "node": "12.16.1"
  },
  "scripts": {
    "build": "concurrently \"cd client && yarn run build\"",
    "clean": "concurrently \"cd server && rimraf node_modules build\" \"cd client && rimraf node_modules build\"",
    "postinstall": "(cd client && yarn install) && (cd server && yarn install)",
    "start": "concurrently \"cd client && yarn run start\" \"cd server && yarn run start\"",
    "start:prod": "cd server && yarn run start"
  },
  "dependencies": {
    "concurrently": "^4.0.1",
    "rimraf": "^2.6.2"
  }
}